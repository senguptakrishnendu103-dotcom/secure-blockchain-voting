const { ethers } = require("ethers");

console.log("resetElection():", ethers.id("resetElection()").substring(0, 10));
console.log("startElection():", ethers.id("startElection()").substring(0, 10));
console.log("voteByAdmin(uint256,address):", ethers.id("voteByAdmin(uint256,address)").substring(0, 10));
